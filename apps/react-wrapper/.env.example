# react-wrapper local development
# Copy to .env in this directory:
#   cp .env.example .env
#
# For Docker, the Dockerfile overrides these values before building.
# See apps/react-wrapper/Dockerfile.

PUBLIC_PROTOCOL=http
PUBLIC_URL=localhost
PUBLIC_PORT=8080
PUBLIC_PATH=api/swagger/doc.json
