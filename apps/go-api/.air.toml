root = "."
tmp_dir = "tmp"

[build]
  cmd = "~/go/bin/swag init -g ./cmd/api/main.go --output ./docs --parseDependency --parseInternal && go build -o ./tmp/api ./cmd/api/"
  bin = "tmp/api"
  include_ext = ["go"]
  exclude_dir = ["tmp", "dist", "node_modules", "docs"]
  delay = 1000
