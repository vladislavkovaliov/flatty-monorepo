# @flatty-budget/sdk type generation
# Copy to .env in this directory:
#   cp .env.example .env
#
# Used by `npm run api` to fetch Swagger docs.

SWAGGER_URL=http://localhost:8080/api/swagger/doc.json
